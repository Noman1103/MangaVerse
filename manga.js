/* let adminBtn = document.getElementById("adminBtn");
let connexBtn = document.getElementById("connexBtn");
let connexBox = document.getElementById("connexBox");
let adminBox = document.getElementById("adminBox");

adminBtn.onclick = function(){
    connexBtn.classList.add('disable');
    adminBtn.classList.remove('disable');
    adminBox.classList.remove('disable');
    connexBox.classList.add('disable');
}

connexBtn.onclick = function(){
    adminBtn.classList.add('disable');
    connexBtn.classList.remove('disable');
    adminBox.classList.add('disable');
    connexBox.classList.remove('disable');
} */
